const createIngredient = (req, res) => { res.status(200).json({ "status": "success" }) }

const deleteIngredient = (req, res) => { res.status(200).json({ "status": "success" }) }

module.exports = {
    createIngredient, deleteIngredient
}